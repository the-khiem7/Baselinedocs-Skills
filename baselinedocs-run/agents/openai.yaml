interface:
  display_name: "Baseline Docs Run"
  short_description: "Execute roadmap phases with checkpoints"
  default_prompt: "Use $baselinedocs-run to execute the active roadmap with phase checkpoints and the requested approval policy."
policy:
  allow_implicit_invocation: false
