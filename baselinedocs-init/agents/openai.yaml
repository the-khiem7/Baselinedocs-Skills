interface:
  display_name: "Baseline Docs Init"
  short_description: "Create the first resumable baseline docs pack"
  default_prompt: "Create the initial baseline documentation pack for this codebase, task, or feature."
