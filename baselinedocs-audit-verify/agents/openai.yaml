interface:
  display_name: "Baseline Docs Internal: Audit Verify"
  short_description: "Verify baseline claims by evidence"
  default_prompt: "Use $baselinedocs-audit-verify to verify baseline claims against evidence."
policy:
  allow_implicit_invocation: true
