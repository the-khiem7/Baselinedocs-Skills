interface:
  display_name: "Baseline Docs Internal: Sync Decision"
  short_description: "Apply one closed decision precisely"
  default_prompt: "Use $baselinedocs-sync-decision to apply one closed decision precisely."
policy:
  allow_implicit_invocation: true
