interface:
  display_name: "Baseline Docs Internal: Audit Drift"
  short_description: "Audit code and docs drift"
  default_prompt: "Use $baselinedocs-audit-drift to audit drift without modifying files."
policy:
  allow_implicit_invocation: true
