interface:
  display_name: "Baseline Docs Internal: Compact"
  short_description: "Compact noisy baseline docs"
  default_prompt: "Use $baselinedocs-maintain-compact to reduce noise while preserving truth."
policy:
  allow_implicit_invocation: true
